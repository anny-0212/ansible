[defaults]
inventory = ./inventory
remote_user = sysadm
ask_pass = false
stdout_callback = debug

[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false 
